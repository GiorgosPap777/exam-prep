# HANDOFF — κατάσταση εργασίας

Τελευταία ενημέρωση: 2026-09-17

Αυτό το αρχείο είναι σημείωμα παράδοσης, όχι τεκμηρίωση. Όταν τελειώσει η δουλειά με
τη Φυσική, σβήσ' το.

---

## 1. Τι είναι έτοιμο και **δεν** έχει γίνει commit

Καμία δέσμευση δεν έχει γίνει ακόμη — ο χρήστης ήθελε πρώτα να κάνει τις μετονομασίες.
Ο σκελετός της μετονομασίας είναι **staged** (`git diff --cached`)· η νέα ύλη Χημείας,
όλη η Φυσική, το `HANDOFF.md` και το `CACHE_VERSION` bump είναι ακόμη **unstaged**
(`git diff`, `git status`). Το `data/physics.json` είναι **untracked**.

### 1α. Μετατροπή σε πολλά μαθήματα + μετονομασία σε «Exam Prep»

Ολοκληρώθηκε και ελέγχθηκε πλήρως (προηγούμενη φάση):

- `questions.json` → `data/chemistry.json` με `git mv`· ο πίνακας `chapters` είναι
  **deep-equal** με πριν.
- Νέο `data/index.json` (κατάλογος μαθημάτων).
- `app.js` ξαναγραμμένο: namespaced ids (`chem/1.87:α`), migration από `chemquiz.v1`
  → `examprep.v2`, τρεις οθόνες επιλογής (αρχική → μάθημα → κεφάλαιο), tri-state
  checkbox κεφαλαίου, `activePool(scope)`, `formatSci` ανά μάθημα.
- `sw.js` με precache **από τον κατάλογο**, network-first στο `/data/`.
- Νέα εικονίδια (✓ σε στρογγυλεμένο πλαίσιο), `manifest`, `Dockerfile`, `nginx.conf`,
  `.dockerignore`, `.claude/launch.json`, `README.md`, `CLAUDE.md`.
- `seed-questions.json` διαγράφηκε.

**Ελέγχθηκαν:** και οι 4 τύποι καρτών, Lewis (`chem/1.89:δ`), αποτελέσματα +
«Εξάσκηση στα λάθη», resume μετά από reload, επιβίωση επιλογής μετά από reload,
migration με πλήρη παλιά κατάσταση, dark mode (contrast 16.4:1), reduced motion,
**offline** (σβηστός server → φορτώνει από cache), **αναβάθμιση cache**
(η παλιά cache σβήνεται, η νέα γεμίζει με το shell + όλα τα data),
`docker build`/`run`/curl (σωστά `Cache-Control`, μηδέν φωτογραφίες στο image).

### 1β. Νέα ύλη Χημείας — **ΟΛΟΚΛΗΡΩΘΗΚΕ**

Από το `raw_data/chemistry/` (βοήθημα οξειδοαναγωγής, **άλλο βιβλίο** από το
«Διαμοριακές Δυνάμεις» — και τα δύο έχουν σελίδα 56 με άλλο περιεχόμενο).

| Κεφάλαιο (id) | Τίτλος | Ασκήσεις | Κάρτες |
|---|---|---|---|
| `1` | Διαμοριακές Δυνάμεις | 1.81–1.94, Κ1.Α, Κ1.Β1 | 92 (προϋπήρχε) |
| `ox1` | Αριθμός οξείδωσης | 1.37–1.45, 1.47, 1.48 | 60 |
| `ox2` | Οξείδωση – Αναγωγή | 2.36–2.45 | 65 |
| `ox3` | Αντιδράσεις οξειδοαναγωγής | 3.51–3.54 | 22 |

**Σύνολο 239 κάρτες, 0 σφάλματα** στον validator.

Απόφαση του χρήστη: **ένα μάθημα «Χημεία», κρυμμένοι οι αριθμοί κεφαλαίων.** Γι' αυτό
τα ids των νέων κεφαλαίων είναι `ox1/ox2/ox3` (τα δύο βιβλία αριθμούν και τα δύο από
το 1) και το `app.js` δείχνει **μόνο τον τίτλο** του κεφαλαίου:
- `renderSubject()` → `<span class="row-title">${esc(ch.title)}</span>`
- `renderChapter()` → `$('chapter-title').textContent = chapter.title;`

Παραλείφθηκε συνειδητά:
- **1.46** — άσκηση διάταξης (ταξινόμηση σε σειρά). Δεν υπάρχει τέτοιος τύπος κάρτας.
- 2.34, 2.35 (πάνω μέρος σελ. 55) — ερωτήσεις **ανοικτού τύπου**.

Οι 2 προειδοποιήσεις του validator (`chem/1.47`, `chem/1.48`: «το ίδιο right πολλές
φορές») είναι **σωστές** — οι αντιστοιχίσεις δεν είναι αμφιμονοσήμαντες. Επαληθεύτηκε
κάθε ζεύγος με υπολογισμό του αριθμού οξείδωσης.

### 1γ. Δύο μικρές αλλαγές στο `app.js` για τη Φυσική

Έγιναν ήδη, με αντίστοιχη χαλάρωση στον validator:

```js
// buildCards, single_choice_set — ερώτηση χωρίς υποερωτήματα
id: base.exKey + (it.label ? ':' + it.label : ''),
label: it.label || null,
```

Δηλαδή μια ερώτηση **μονής απάντησης** (όπου τα α/β/γ/δ είναι οι **επιλογές**, όχι
υποερωτήματα) γράφεται με **ένα item χωρίς `label`**, παίρνει id σκέτο `phys/1.11`
και το badge δείχνει «1.11» χωρίς ψεύτικο «· α». Ο validator απαιτεί labels μόνο όταν
τα items είναι 2+.

---

## 2. Φυσική — τι μπήκε και τι λείπει

### Κανόνες που έθεσε ο χρήστης (κρίσιμοι — δες και `memory/`)

0. **Μία συνεδρία = ένα μάθημα.** Δεν υπάρχει ανάμεικτη εξάσκηση. Η αρχική οθόνη δεν
   έχει καθόλου «Έναρξη» — μόνο «Συνέχεια», και κρύβεται όταν δεν υπάρχει μισοτελειωμένη
   συνεδρία. Το `activePool(scope)` βγάζει **πάντα** το μάθημα από το scope (ακόμη και
   από το `chapterKey`, που το κουβαλάει: `phys/9`) και **επιστρέφει κενό** αν δεν βρει
   μάθημα — έτσι ούτε λάθος κλήση δεν μπορεί να γεννήσει ανάμεικτη συνεδρία.

1. **Μόνο γρήγορες ερωτήσεις.** Ό,τι απαιτεί χαρτί και υπολογισμούς **δεν μπαίνει
   καθόλου** — ούτε ως placeholder.
2. **Όχι σχήματα.** Η εφαρμογή δεν κάνει render διαγράμματα. Αν η εκφώνηση δεν στέκει
   χωρίς την εικόνα, παραλείπεται. Αν το σχήμα είναι απλώς επεξηγηματικό, κρατιέται
   και η διάταξη **περιγράφεται με λόγια** (π.χ. «πυροβόλο μάζας M και βλήμα μάζας m»
   αντί για «του σχήματος»).

### Τι είναι ήδη στην τράπεζα — `data/physics.json`

| Κεφ. | Τίτλος | Ασκήσεις | Κάρτες |
|---|---|---|---|
| `1` | Ορμή | 1.8–1.29 | 72 |
| `2` | Ελαστική κρούση (I) | 2.21–2.39 | 42 |
| `5` | Ελαστική κρούση (IV) | 5.8–5.17 | 36 |
| `7` | Ανελαστική μετωπική κρούση (I) | 7.25–7.42 | 40 |
| `9` | Ανελαστική μετωπική κρούση (III) | 9.5–9.6 | 16 |
| `10` | Πλάγια ή μη μετωπική κρούση | 10.15–10.26, 10.28 | 25 |
| `11` | Απλή αρμονική ταλάντωση (I) | 11.50–11.68 | 25 |
| `12` | Απλή αρμονική ταλάντωση (II) | 12.20–12.31 | 23 |
| `13` | Α.Α.Τ. και ελατήρια | 13.22–13.29 | 23 |

**Σύνολο Φυσικής 302 κάρτες. Όλη η τράπεζα 541 κάρτες, 0 σφάλματα.**

Το κεφ. 9 έχει μόνο 2 ασκήσεις γιατί η 9.6 είναι ένα «τέρας»: 12 προτάσεις σε τρεις
φάσεις (πτώση / κρούση / διαδρομή Α→Γ). Τα labels της είναι `Α1…Α5`, `Β1…Β3`, `Γ1…Γ4`
και κάθε πρόταση ξεκινά με τη φάση της, ώστε η κάρτα να στέκει μόνη της.

Παραλείφθηκαν συνειδητά:

| Άσκηση | Λόγος |
|---|---|
| 1.30, 1.34, 11.78–11.79, 12.32+, 13.30+ | ενότητα **«Ερωτήσεις ανοικτού τύπου»** |
| 1.31–1.33 | **δεν μπαίνουν ποτέ** — απόφαση χρήστη: θέλουν χαρτί και μολύβι |
| 10.27 | υπολογιστική (διανυσματική πρόσθεση, Πυθαγόρειο, ενεργειακό ισοζύγιο) |
| 5.18, 9.7–9.10 | ενότητα **«Ερωτήσεις ανοικτού τύπου»** (και η 5.18 μισοκρυμμένη από δάχτυλα) |
| 9.6 `β-iv` | το μόνο **item** που κόπηκε μεμονωμένα: `dp_2/dt = m_2^2g/(m_1+m_2)` θέλει αλυσίδα `kx_1 = m_1g` → `ΣF` → `a` — χαρτί |
| 11.58, 11.69, 11.70, 11.72, 11.75 | εύρεση **αρχικής φάσης** — απαιτεί επιλογή κλάδου στο ημίτονο, δουλειά με χαρτί |
| 11.62, 11.64, 11.71, 11.73, 11.74, 11.76, 11.77 | **γραφικές παραστάσεις / σχήματα** |
| 12.28, 12.29, 12.30 | **γραφικές παραστάσεις** |
| 11.70 (και το 11.75) | οι επιλογές τους βρίσκονται σε μη φωτογραφημένη σελίδα |

Έλεγχος που τρέχει σε κάθε προσθήκη — μηδέν αποτελέσματα σημαίνει καθαρή τράπεζα:

```bash
python3 - <<'EOF'
import json, io, re
d = json.load(io.open('data/physics.json', encoding='utf-8'))
pat = re.compile(r'σχήμα|διάγραμμα|γραφικ|υπολογίστε|να αποδείξ|να βρείτε')
for ch in d['chapters']:
    for q in ch['questions']:
        blobs = [q['instructions']]
        for it in q['items']:
            blobs.append(it.get('text') or it.get('stem') or '')
            blobs += [str(v) for v in (it.get('options') or {}).values()]
        for t in blobs:
            if pat.search(t): print(q['id'], '|', t[:150])
EOF
```

Οι 2 προειδοποιήσεις του validator (`phys/1.21`, `phys/2.22`: «όλες οι προτάσεις είναι
σωστές») είναι **σωστές** — έτσι το λέει το τυπωμένο κλειδί.

### Οριακές περιπτώσεις που κρατήθηκαν

Λύνονται με το μυαλό, αλλά είναι οι πιο «υπολογιστικές» της τράπεζας. Αν ο χρήστης τις
βρει κουραστικές, αυτές φεύγουν πρώτες:
`2.36:α` (3mv_0 − mv_0), `11.55` (ω^2 = 16π^2 → f), `11.66` (v στο x = A/2),
`13.24` (T^2 = 3^2 + 4^2), `5.13:δ` (ενεργειακό ισοζύγιο αργής ανύψωσης),
`5.11:δ` (αναγνώριση του όρου ½kx_1^2 που λείπει).

### Υλικό στο `raw_data/` (όλα gitignored — σαρώσεις βιβλίου με copyright)

| Φάκελος | Τι είναι |
|---|---|
| `physics/` | 32 φωτογραφίες **εκφωνήσεων**, σελ. 25–329 |
| `pysics-ansers-some/` | 12 φωτογραφίες **απαντήσεων** (μερικές — έρχονται κι άλλες) |
| `additives-exap-perrrr/` | 4 φωτογραφίες **απαντήσεων** κεφ. 11, 12, 13 — **αξιοποιήθηκαν** |
| `chemistry/` | 15 φωτογραφίες — **αξιοποιήθηκαν πλήρως** |
| `old-photos-form-the-root-of-the-dir/` | οι αρχικές του πρώτου βιβλίου Χημείας — ήδη στην τράπεζα |

### Χάρτης φωτογραφιών → σελίδων (`raw_data/physics/`, αλφαβητική σειρά)

```
01 IMG_20260908_142037  περιεχόμενα     17 IMG_20260912_000203  197
02 IMG_20260908_142048  περιεχόμενα     18 IMG_20260912_000217  198
03 IMG_20260911_235312   25             19 IMG_20260912_000245  228  (χωρίς περιστροφή)
04 IMG_20260911_235340   26             20 IMG_20260912_000255  229
05 IMG_20260911_235349   27             21 IMG_20260912_000306  230
06 IMG_20260911_235545   28             22 IMG_20260912_000344  278
07 IMG_20260911_235613   47             23 IMG_20260912_000355   —
08 IMG_20260911_235704   48  (cw)       24 IMG_20260912_000404  279
09 IMG_20260911_235712   49             25 IMG_20260912_000414   —
10 IMG_20260911_235844  104             26 IMG_20260912_000425  303
11 IMG_20260911_235858  105             27 IMG_20260912_000509  304
12 IMG_20260911_235916  106             28 IMG_20260912_000523  305
13 IMG_20260912_000018   —             29 IMG_20260912_000531  327
14 IMG_20260912_000027  145             30 IMG_20260912_000602  328
15 IMG_20260912_000043  146             31 IMG_20260912_000620   —
16 IMG_20260912_000058  147             32 IMG_20260912_000630  329
```

Οι απαντήσεις (`raw_data/pysics-ansers-some/`) είναι σε σειρά:
`102417→577, 102427→578, 102511→583, 102531→584 (ccw), 102627→615, 102641→616,
102737→643, 102820→644, 103000→671, 103019→674, 103137→700, 103159→701`.

### Τι απομένει

**Τίποτα από τις φωτογραφίες που υπάρχουν.** Κάθε ζεύγος εκφωνήσεων/απαντήσεων που
έχει φωτογραφηθεί έχει αποδελτιωθεί: 1, 2, 5, 7, 9, 10, 11, 12, 13.

### Το μοναδικό κενό: κλειδί για την 9.6 `Γ1–Γ4`

Το κλειδί της 9.6 κόβεται στη σελ. **671** (τελειώνει στο `β-iv`). Η συνέχεια είναι
στη σελ. **672**, που **δεν έχει φωτογραφηθεί** — η επόμενη φωτογραφία είναι η 674.

Οι 4 προτάσεις `Γ1–Γ4` μπήκαν με **δική μου τεκμηρίωση**, όχι από το τυπωμένο κλειδί:

| | πρόταση | απάντηση | γιατί |
|---|---|---|---|
| `Γ1` | η ορμή του συσσωματώματος διατηρείται | Λάθος | μηδενίζεται στο Γ |
| `Γ2` | ΔK = ΔU ελατηρίου | Λάθος | έργο παράγει και το βάρος |
| `Γ3` | στο Γ ο ρυθμός μεταβολής της K είναι 0 | **Σωστό** | `dK/dt = ΣF·v` και `v = 0` |
| `Γ4` | σε όλη τη διαδρομή η ταχύτητα μειώνεται | Λάθος | αμέσως μετά την κρούση επιταχύνεται |

Αν έρθει φωτογραφία της σελ. 672, διασταύρωσέ τες. Οι υπόλοιπες 12 κάρτες του κεφ. 9
είναι ελεγμένες με το τυπωμένο κλειδί.

### Τι πρέπει να ζητηθεί από τον χρήστη

Φωτογραφία της σελ. **672** (κλειδί), για τη διασταύρωση παραπάνω.
Προαιρετικά, αν θέλει μεγαλύτερη κάλυψη στα Α.Α.Τ.:
σελ. **282+**, **306+** και **330+** (εκφωνήσεις 11.71→, 12.32→, 13.30→) — τα κλειδιά
τους υπάρχουν ήδη. Προσοχή όμως: από εκεί και πέρα αρχίζουν οι «Ερωτήσεις ανοικτού
τύπου», που ούτως ή άλλως δεν μπαίνουν.

**Η σελ. 29 δεν χρειάζεται** — ο χρήστης αποφάσισε ότι οι 1.31–1.33 θέλουν χαρτί και
μολύβι, άρα μένουν εκτός για πάντα.

---

## 3. Πρακτικά

Διόρθωση προσανατολισμού πριν το διάβασμα (πολλές φωτογραφίες είναι πλάγιες, και
**όχι πάντα προς την ίδια φορά** — φτιάξε και τις δύο και δες ποια διαβάζεται):

```bash
ffmpeg -i IN.jpg -vf "transpose=2,scale=1100:-1:flags=lanczos" out_ccw.png
ffmpeg -i IN.jpg -vf "transpose=1,scale=1100:-1:flags=lanczos" out_cw.png
```

Γρήγορη εύρεση αριθμού σελίδας χωρίς να διαβάσεις όλη τη φωτογραφία — κόψε την κάτω
λωρίδα και στοίβαξέ τις:

```bash
ffmpeg -i IN.jpg -vf "crop=W:H*7/100:0:H*93/100,scale=900:-1" strip.png
```

### Κατάσταση repo

- Ο φάκελος έχει ήδη μετονομαστεί σε `exam-prep` και το `git remote` δείχνει στο
  `git@github.com:GiorgosPap777/exam-prep.git`. ✅
- **Εκκρεμεί:** commit + push, και νέο Docker Hub repo `giorgospap777/exam-prep`
  (το Docker Hub δεν υποστηρίζει μετονομασία — χρειάζεται νέο και διαγραφή του παλιού).
- `CACHE_VERSION = 'examprep-v8'`. Ο service worker επιβεβαιώθηκε ότι κάνει precache
  και το `data/physics.json` — τη λίστα τη χτίζει από τον κατάλογο, δεν τη γράφεις.
- Τα `raw_data/**/*.jpg` είναι gitignored και εκτός Docker build context. **Να μείνουν
  εκτός** — είναι σαρώσεις βιβλίου με copyright.

# Exam Prep — οδηγίες για το project

Phone-first PWA για εξάσκηση σε ερωτήσεις από σχολικά βιβλία, **σε πολλά μαθήματα**.
Plain HTML/CSS/JS, χωρίς build step και χωρίς εξαρτήσεις.

```
index.html  styles.css  app.js      # η εφαρμογή
data/index.json                     # ο κατάλογος των μαθημάτων
data/chemistry.json                 # ένα αρχείο ανά μάθημα (source of truth)
manifest.webmanifest  sw.js  icons/ # PWA
tools/validate-questions.mjs        # έλεγχος ακεραιότητας
tools/make-icons.mjs                # παραγωγή εικονιδίων (τρέχει μία φορά)
```

Τοπικά: `python3 -m http.server 8000` και άνοιγμα του `http://localhost:8000`
(το `file://` δεν δουλεύει — χρειάζεται fetch και secure context για τον service worker).

---

## Προσθήκη νέων φωτογραφιών στην τράπεζα

Ο χρήστης θα βάζει κατά καιρούς νέες φωτογραφίες σελίδων στον φάκελο του project.
Πρώτα βρες **σε ποιο μάθημα** ανήκουν — αν δεν είναι προφανές από το περιεχόμενο,
ρώτα. Μετά, η διαδικασία κάθε φορά:

### 0. Δύο φίλτρα — τι ΔΕΝ μπαίνει ποτέ

Η τράπεζα είναι για **γρήγορη εξάσκηση στο κινητό**, όχι για λύσιμο ασκήσεων.

1. **Μόνο ό,τι λύνεται με το μυαλό.** Αν η άσκηση θέλει χαρτί και μολύβι —
   στήσιμο και λύση εξίσωσης, διανυσματική ανάλυση, επιλογή κλάδου σε τριγωνομετρική
   συνάρτηση, ενεργειακό ισοζύγιο — **δεν μπαίνει καθόλου**, ούτε ως placeholder.
   Ορίζεται με έναν πρακτικό κανόνα: κρατάς ό,τι απαντιέται με **ορισμό, πρόσημο,
   αναλογία, ανάγνωση τύπου ή αριθμητική με μικρούς ακέραιους**.
2. **Όχι σχήματα.** Η εφαρμογή δεν κάνει render διαγράμματα. Αν η εκφώνηση δεν στέκει
   χωρίς την εικόνα (γραφική παράσταση, τροχιά, διάταξη ελατηρίων), παραλείπεται. Αν
   το σχήμα είναι απλώς επεξηγηματικό, κρατιέται και η διάταξη **περιγράφεται με
   λόγια** — π.χ. «πυροβόλο μάζας M και βλήμα μάζας m» αντί για «του σχήματος».

Το φιλτράρισμα γίνεται **ανά υποερώτημα**, όχι ανά άσκηση: κάθε item γίνεται δική του
κάρτα, οπότε μια καλή άσκηση με ένα υπολογιστικό σκέλος κρατιέται χωρίς αυτό.

Πάντα ανάφερε **πόσα παραλείφθηκαν και γιατί** — ποτέ σιωπηλή απόρριψη.

### 1. Διόρθωσε τον προσανατολισμό πριν διαβάσεις

Πολλές φωτογραφίες — ειδικά των σελίδων με τις **απαντήσεις** — βγαίνουν στο πλάι.
Υπάρχει `ffmpeg` στο σύστημα (δεν υπάρχει ImageMagick/PIL):

```bash
ffmpeg -i IN.jpg -vf "transpose=2" out_ccw.png     # 90° αριστερόστροφα
ffmpeg -i IN.jpg -vf "transpose=1" out_cw.png      # 90° δεξιόστροφα
ffmpeg -i IN.jpg -vf "hflip,vflip"  out_180.png    # 180°
```

Για μεγέθυνση σε δύσκολο σημείο, crop + upscale:

```bash
ffmpeg -i IN.png -vf "crop=W:H:X:Y,scale=2000:-1:flags=lanczos" zoom.png
```

Μην προσπαθήσεις να διαβάσεις πλάγια φωτογραφία «με το μάτι» — η σειρά των γραμμών
μπερδεύεται εύκολα και οδηγεί σε λάθος αντιστοίχιση απαντήσεων.

### 2. Μετέγραψε πιστά στα ελληνικά

Διατήρησε την αρχική διατύπωση, τα σύμβολα και τους δείκτες. Στο JSON γράφονται
**σκέτα ASCII** — η `formatSci()` στο `app.js` τα κάνει render με `<sub>` / `<sup>`.
Μη βάζεις Unicode δείκτες στο JSON. Βλ. «Μορφοποίηση τύπων» πιο κάτω.

Συμβάσεις για ό,τι δεν γράφεται γραμμικά:

| Στο βιβλίο | Στο JSON |
|---|---|
| διακλαδισμένος τύπος με κάθετο δεσμό | `CH3CH(CH3)CH2CH3` |
| κετόνη με `=O` από κάτω | `CH3CH2COCH3` |
| δείκτες κατάστασης | `H2O(s)`, `H2O(ℓ)`, `Fe2+ (aq)` |
| διάγραμμα Lewis | `{ "lewis": { "symbol": "Α", "top": 1, "right": 1, "bottom": 1, "left": 1 } }` |
| εκθέτης / δείκτης εκτός χημείας | `m/s^2`, `10^-11`, `v_0`, `a_{max}` |

### 3. Διασταύρωσε το κλειδί με το αντικείμενο

**Πάντα** έλεγξε ότι η απάντηση του κλειδιού βγάζει νόημα με βάση το κείμενο των
επιλογών. Αν διαφωνούν, ανάφερέ το στον χρήστη — μην το «διορθώσεις» σιωπηλά.

Δύο παγίδες που έχουν ήδη εμφανιστεί:

- **Το κλειδί σπάει σε δύο γραμμές.** Μια ουρά `ε) … στ) … ζ)` ανήκει στην άσκηση
  της **προηγούμενης** γραμμής, όχι της επόμενης.
- **Οι ασκήσεις σπάνε σε στήλες/σελίδες.** Τα υποερωτήματα στην κορυφή της επόμενης
  σελίδας μπορεί να ανήκουν στην προηγούμενη άσκηση. Ο πιο αξιόπιστος έλεγχος είναι
  να συμφωνεί το **πλήθος** των υποερωτημάτων με το πλήθος των απαντήσεων του κλειδιού.

### 4. Γράψε στο αρχείο του μαθήματος

- Ποτέ μη διπλογράφεις υπάρχον exercise id **μέσα στο ίδιο μάθημα** — αν ξαναέρθει
  φωτογραφία για κάτι που υπάρχει ήδη, **ενημέρωσέ το επί τόπου**. (Ίδιο id σε
  **διαφορετικά** μαθήματα είναι μια χαρά: τα ids είναι namespaced.)
- Άσκηση με κλειδί αλλά χωρίς εκφώνηση: `"needs_question_text": true` + `note`.
  Κρατάς το `correct` / `correct_labels`. Δεν εμφανίζεται στην εξάσκηση, εμφανίζεται
  στην οθόνη «Λείπουν κείμενα».
- Νέο κεφάλαιο → νέο αντικείμενο στο `chapters` του μαθήματος.

### 5. Έλεγξε και ανάφερε

```bash
node tools/validate-questions.mjs
```

Μηδέν σφάλματα, και το πλήθος καρτών να ταιριάζει με ό,τι περιμένεις. Μετά ανάφερε
στον χρήστη με λίγα λόγια τι προστέθηκε/άλλαξε — ποτέ σιωπηλή επεξεργασία.

Αν άλλαξε οτιδήποτε κάτω από το `data/`, ανέβασε το `CACHE_VERSION` στο `sw.js` ώστε
να πάρουν οι ήδη εγκατεστημένες συσκευές τη νέα τράπεζα.

---

## Προσθήκη νέου μαθήματος

1. Νέο αρχείο `data/<μάθημα>.json`:

   ```jsonc
   { "id": "phys", "title": "Φυσική", "chapters": [ /* όπως και της Χημείας */ ] }
   ```

2. Μία γραμμή στο `data/index.json`:

   ```jsonc
   { "id": "phys", "title": "Φυσική", "file": "data/physics.json", "color": "#0EA5E9" }
   ```

   Το `id` πρέπει να είναι **ίδιο** μέσα στο αρχείο και στον κατάλογο — ο validator το
   ελέγχει, γιατί από αυτό βγαίνουν τα κλειδιά του `localStorage`.
   Το `color` είναι προαιρετικό (fallback σε παλέτα ανά σειρά) και χρησιμοποιείται
   **μόνο** για το chip του μαθήματος και το δαχτυλίδι προόδου του — όχι για το global
   accent, που θα άλλαζε χρώμα σε κάθε κάρτα μιας ανάμεικτης συνεδρίας.
   Το `autoFormat` μπαίνει **μόνο** στη Χημεία (βλ. πιο κάτω).

3. `node tools/validate-questions.mjs` και `CACHE_VERSION` bump.

Ο service worker χτίζει τη λίστα precache **από τον κατάλογο**, οπότε το νέο αρχείο
μπαίνει στο offline cache χωρίς άλλη αλλαγή.

### Namespacing των ids

Τα ids των καρτών είναι τα κλειδιά στο `localStorage` και **φέρουν πρόθεμα μαθήματος**:

| | παράδειγμα |
|---|---|
| κάρτα | `chem/1.87:α` |
| κάρτα αντιστοίχισης | `chem/1.83` |
| κεφάλαιο | `chem/1` |
| άσκηση | `chem/1.87` |

Γι' αυτό η Φυσική μπορεί να έχει κι αυτή άσκηση `1.87`. **Μην αλλάξεις ούτε τα subject
ids ούτε τα exercise ids** — χάνεται η πρόοδος του χρήστη.

---

## Μορφοποίηση τύπων (`formatSci`)

| | κανόνες |
|---|---|
| **σε κάθε μάθημα** | ρητό `^2`, `^-11`, `^{n+1}` → `<sup>` · `_0`, `_{max}` → `<sub>` |
| **μόνο με `"autoFormat": "chemistry"`** | σιωπηροί κανόνες: `CO2`, `Fe2+`, `[Cu(NH3)4]2+`, `Mr`, `H2O(s)` |

Ο σιωπηρός κανόνας «ψηφίο μετά από λατινικό γράμμα → δείκτης» είναι σωστός για χημεία
και **λάθος** για φυσική (`m/s2` θα γινόταν `m/s₂` αντί για `m/s²`). Γι' αυτό κάθε
μάθημα εκτός της Χημείας γράφει **ρητά** `^` και `_`.

Το `^`/`_` πιάνει μόνο όταν ακολουθείται από `{…}` ή από ψηφία/πρόσημο, ώστε μια σκέτη
κάτω παύλα σε ελληνικό κείμενο να μη μεταμορφώνεται.

---

## Πλοήγηση: μία συνεδρία = ένα μάθημα

Η εξάσκηση ζει **πάντα μέσα σε ένα μάθημα**. Ο χρήστης διαλέγει πρώτα μάθημα και μετά
πατάει «Έναρξη» — δεν υπάρχει ανάμεικτη συνεδρία σε όλη την τράπεζα.

- Το `activePool(scope)` δέχεται `{subjectId}` ή `{chapterKey}`, ποτέ σκέτο `{}`.
- Η αρχική οθόνη έχει στη μπάρα **μόνο** το «Συνέχεια», και η ίδια η μπάρα
  (`#home-actions`) κρύβεται όταν δεν υπάρχει μισοτελειωμένη συνεδρία — αλλιώς μένει
  ένα κενό ταμπλό στο κάτω μέρος.
- Το «Εξάσκηση στα λάθη» δουλεύει πάνω στις κάρτες της συνεδρίας που μόλις τελείωσε,
  οπότε παραμένει αυτόματα μέσα στο μάθημα.

Αν προστεθεί λειτουργία που μαζεύει κάρτες (global review, shuffle), **κάν' τη ανά
μάθημα**.

## Εμφάνιση

Design tokens στην κορυφή του `styles.css` (light + dark μέσω `prefers-color-scheme`):
accent gradient `#6366F1 → #8B5CF6`, `--ok`, `--bad`, `--r*` για γωνίες, `--shadow-1/2/3`,
`--ease-out` / `--ease-spring`. Μη γράφεις σκέτα χρώματα — πάντα μέσω token, αλλιώς
σπάει το dark mode. (Εξαίρεση: το `color` του μαθήματος, που περνάει από `safeColor()`
πριν μπει σε inline style.)

Τρεις παγίδες που έχουν ήδη χτυπήσει:

- **Ποτέ `transform` σε πρόγονο του `.actions-sticky`.** Οποιοδήποτε `transform`
  (ακόμη και identity, ακόμη και από `animation-fill-mode: both`) κάνει τον πρόγονο
  containing block για τα `position: fixed` παιδιά του, και η μπάρα φεύγει εκτός οθόνης.
  Γι' αυτό το `screenIn` κάνει animate μόνο `opacity`.
- **Όλα τα animations έχουν guard** σε `@media (prefers-reduced-motion: reduce)`.
  Όλα τα keyframes γράφονται **μόνο με `from`**, ώστε το τελικό state να είναι το
  φυσικό state του στοιχείου· έτσι τίποτα δεν μένει μόνιμα αόρατο αν δεν τρέξει ένα
  animation.
- **Όχι `:has()`.** Σε engine χωρίς υποστήριξη πέφτει ολόκληρος ο κανόνας σιωπηλά.
  Χρησιμοποίησε modifier class (π.χ. `.row-card.row-split`).

Αν αλλάξεις shell αρχείο (`index.html`/`styles.css`/`app.js`), ανέβασε το `CACHE_VERSION`
στο `sw.js`. Ο service worker κάνει `addAll` με `cache: 'reload'`, δεν cachάρει τον εαυτό
του, σερβίρει το cached `index.html` σε κάθε navigation, και είναι **network-first** για
ό,τι είναι κάτω από `/data/`.

## Σχήμα των αρχείων

`data/index.json`:

```jsonc
{
  "version": 2,
  "subjects": [
    { "id": "chem", "title": "Χημεία", "file": "data/chemistry.json",
      "color": "#6366F1", "autoFormat": "chemistry" }
  ]
}
```

`data/<μάθημα>.json`:

```jsonc
{
  "id": "chem",
  "title": "Χημεία",
  "chapters": [{
    "id": "1", "title": "…", "source": "…",
    "questions": [
      { "id": "1.87", "type": "single_choice_set", "instructions": "…",
        "items": [ { "label": "α", "stem": "…",
                     "options": { "i": "…", "ii": "…" },   // ή { "lewis": {…} }
                     "correct": "iii", "explanation": "…" } ] },

      { "id": "1.84", "type": "true_false_set", "instructions": "…",
        "items": [ { "label": "α", "text": "…", "explanation": "…" } ],
        "correct_labels": ["α", "β"] },                     // οι ΣΩΣΤΕΣ· οι υπόλοιπες λάθος

      { "id": "1.83", "type": "matching", "instructions": "…",
        "left":  [ { "label": "α", "text": "…", "explanation": "…" } ],
        "right": [ { "label": "i", "text": "…" } ],
        "correct": { "α": "iv" } }
    ]
  }]
}
```

`explanation` και `section` είναι προαιρετικά. Το `source` του κεφαλαίου εμφανίζεται
κάτω από τον τίτλο· αν ξεκινά με το όνομα του μαθήματος, το πρόθεμα παραλείπεται για
να μη γραφτεί δύο φορές. Τα labels είναι είτε ελληνικά (`α β γ δ ε στ ζ η θ ι`) είτε
λατινικά αριθμητικά (`i…x`) — η `sortLabels()` τα ταξινομεί σωστά· για ό,τι άλλο
(π.χ. `Α1…Α6`) κρατά τη σειρά του αρχείου.

### Πώς γίνονται κάρτες

- `single_choice_set` και `true_false_set`: **μία κάρτα ανά υποερώτημα**, με σταθερό id
  `"<subject>/<exercise>:<label>"` (π.χ. `chem/1.87:α`). Κάθε πρόταση/ερώτημα λύνεται μόνη της.
- `matching`: **μία κάρτα για ΟΛΗ την άσκηση**, με id `"<subject>/<exercise>"`
  (π.χ. `chem/1.83`). Οι αντιστοιχίσεις λύνονται συγκριτικά — βλέπεις όλη τη στήλη Α
  μαζί, συγκρίνεις («αυτό βράζει ψηλότερα από εκείνο») και αποκλείεις. Αν σπάσει σε μία
  κάρτα ανά γραμμή, η άσκηση γίνεται άλυτη. Μην το αλλάξεις πίσω.
  Βαθμολογείται όλο μαζί: σωστό μόνο αν πέσουν σωστά όλα τα ζευγάρια (το feedback δείχνει
  «ν από Ν σωστές αντιστοιχίσεις»).

Τα ids είναι τα κλειδιά στο localStorage — μην τα αλλάξεις.
